using UnityEngine;

public class MemberVisual2 : MonoBehaviour
{

    public ClusterVisual2 ClusterVisual;

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
